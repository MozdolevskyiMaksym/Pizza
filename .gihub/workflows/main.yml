name: Eslint check

on:
  pull_request:
    branches:
      - develop

jobs:
  eslint:
    name: Eslint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Install dependencies
        run: npm ci
      - name: Run eslint
        run: npm run eslint